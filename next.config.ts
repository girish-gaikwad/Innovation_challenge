import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["media.licdn.com","www.skydo.com","encrypted-tbn0.gstatic.com", "images.stockcake.com","upload.wikimedia.org","static.cdnlogo.com"],
  },
};

export default nextConfig;
